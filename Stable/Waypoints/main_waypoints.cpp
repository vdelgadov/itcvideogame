#include <iostream>
#include "../Waypoints/Waypoint.h"

using namespace std;
int main(){
/*	Waypoint* a = new Waypoint("a", Vector2D());
	Waypoint* b = new Waypoint("b", Vector2D(3.0, 0.0));
	Waypoint* c = new Waypoint("c", Vector2D(0.0, 3.0));
	Waypoint* d = new Waypoint("d", Vector2D(4.0, 1.0));

	a->addNbor(b);
	b->addNbor(a);
	a->addNbor(c);

	d->addNbor(b);
	b->addNbor(d);

	b->getPos().magnitude();
	cout << a->isNbor(b) << endl;
//	SteeringBehaviors::seek(Vector2D(), new Vehicle());

	while(1){}*/
}